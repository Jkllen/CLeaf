// manual_add_plant_screen.dart
import 'package:flutter/material.dart';

class ManualAddPlantScreen extends StatelessWidget {
  const ManualAddPlantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Manually Add Plant")),
      body: const Center(child: Text("Manual input feature coming soon.")),
    );
  }
}
