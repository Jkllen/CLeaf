// upload_plant_screen.dart
import 'package:flutter/material.dart';

class UploadPlantScreen extends StatelessWidget {
  const UploadPlantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Upload Plant Image")),
      body: const Center(child: Text("Upload image feature coming soon.")),
    );
  }
}
